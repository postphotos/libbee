# Python
__pycache__/
*.py[cod]
*$py.class
*.so
build/
dist/
*.egg-info/
*.egg

# Virtual environments
.venv/
venv/

# Data & databases — the 266MB build artifact is regenerated by `libbee build`.
# Only the reproducibility fingerprint is tracked.
data/*
!data/MANIFEST.json
*.db
*.sqlite
*.sqlite3
*.duckdb

# Secrets (Census API key etc.) — used by `libbee build`, never committed
.env
.env.local

# Extra local docs
.docs

# Caches / IDE
.pytest_cache/
.ruff_cache/
.coverage
.marimo/
__marimo__/
.vscode/
.idea/
*.swp
.DS_Store
